$schema: "1.0"
name: "secrets"
image: microsoftvisualstudio_visualstudioplustools_vs-2022-ent-general-win11-m365-gen2
description: "uses a secret"

tasks:
  - name: powershell
    parameters:
      command: |
        Write-Host "Hello world!"
        Write-Host "The password is '{{https://maxh-euap-compute-kv.vault.azure.net/secrets/password}}'"
        Write-Host "az login --service-principal --username foo --password '{{https://maxh-euap-compute-kv.vault.azure.net/secrets/password}}'"
        az login --service-principal --username foo --password '{{https://maxh-euap-compute-kv.vault.azure.net/secrets/password}}'
